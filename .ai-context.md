# .ai-context.md

AIエージェントが作業開始時に現在の状態・進行中Issue・次の手を把握するためのコンテキストファイル。
**作業開始前に必ず読むこと。作業終了時に必ず更新すること。**

---

## 1. Status

`works (Initializing)`
- プロジェクト資材の配置が完了し、`Diffairy` プロジェクトへの準拠化作業中。

---

## 2. Active Issues

- [ ] Project Setup: 資材のプロジェクト準拠化（名称、URL等の修正）

---

## 3. Decisions

- [2026-03-11] テンプレート資材をルート直下に配置。
- [2026-03-11] 作業リポジトリ `Diffairy` (Swift/SwiftUI) に合わせて `os-template.yml` を設定。

---

## 4. Next actions

1. `.ai-instructions.md` のリファレンス確認と修正。
2. `scripts/` 内の「AI-First Development OS」参照を `Diffairy` に修正。
3. `docs/` 内の名称修正。


---

## 5. Known pitfalls

<!-- 過去の失敗・詰まりポイントを蓄積する。新たな罠を発見したらここに追記する -->

- `GitHub Action (trufflehog) が signal: killed でサイレント終了する不具合があるため、Gitleaks等を利用するかベースブランチの明示的なfetchが必要`
- `pre-push hook はホスト環境(macOS/Linuxなど)によってawkやgrepの挙動が変わる可能性があるため、依存を減らしたシンプルなスクリプト記述を心がける`

---

## 6. Commands

<!-- このリポジトリは OS Template Repo のため、各開発プロジェクトにおける固有コマンドは、ここから派生した Instance 側のリポジトリで設定・運用されます。 -->
<!-- 当リポジトリ（OS自体）のCI/CDやローカル検証用としては、以下のコマンドを利用します。 -->

| コマンド | 実行方法 | 備考 |
|---|---|---|
| doctor | `./scripts/run doctor` | `環境依存関係（python, bash, gitなど）のチェック` |
| ci | `./scripts/run ci` | `PR作成時等に実行される各種チェック・テストのフローマスター` |
| format | `./scripts/run format` | `コードフォーマットの実行` |
| lint | `./scripts/run lint` | `静的解析の実行` |
| typecheck | `./scripts/run typecheck` | `将来の拡張用・現在は未実装` |
| test | `./scripts/run test` | `単体テスト（Python unittest）の実行` |
| build | `./scripts/run build` | `将来の拡張用・現在は未実装` |

---

## 7. CI Notes

<!-- TODO: CI が落ちた時の典型原因と対処法を記載する。繰り返し発生するものを優先的に記録する -->
